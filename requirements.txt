# ---- 运行必需 ----
fastapi>=0.110,<1.0
uvicorn>=0.27,<1.0
SQLAlchemy>=2.0,<3.0
pydantic>=2.5,<3.0
python-multipart>=0.0.9          # 附件上传（multipart 表单解析）
numpy>=1.26
scikit-learn>=1.3                # 协同过滤与 TruncatedSVD
matplotlib>=3.8                  # evaluate.py 离线评估图表

# ---- 测试脚本需要（tools/ 下的 UI 巡检、界面截图、链接可达性巡检）----
websocket-client>=1.7
